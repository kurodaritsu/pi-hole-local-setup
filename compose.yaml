# For more details, see https://docs.pi-hole.net/docker/

services:
  pihole:
    container_name: pihole
    image: pihole/pihole:latest
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "80:80/tcp"
    environment:
      TZ: ${TZ}
      FTLCONF_webserver_api_password: ${ADMIN_PASSWORD}
      FTLCONF_dns_listeningMode: 'ALL'
      FTLCONF_dns_upstreams: '1.1.1.1;1.0.0.1;2606:4700:4700::1111;2606:4700:4700::1001'
      FTLCONF_database_maxDBdays: '90'
    volumes:
      - ./etc-pihole:/etc/pihole
      - ./resolv.conf:/etc/resolv.conf:ro
    user: "0:0"
    restart: unless-stopped
    networks:
      - pihole

networks:
  pihole:
    enable_ipv6: true
    ipam:
      config:
        - subnet: 10.89.2.0/24
        - subnet: fd00:89:2::/64
